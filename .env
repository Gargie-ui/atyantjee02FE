# In development, Vite proxies /api → http://localhost:5000
# Leave VITE_API_URL empty in dev (proxy handles it automatically)
# In production (Vercel/Nginx), set this to your backend URL:
# VITE_API_URL=https://api.atyant.in
VITE_API_URL=
